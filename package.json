{
  "name": "exfmt-atom",
  "main": "./lib/exfmt-atom",
  "version": "0.0.0",
  "description": "Atom package to format Elixir code using the exfmt formatter.",
  "keywords": ["elixir", "formatter", "exfmt"],
  "activationCommands": {
    "atom-workspace": "exfmt-atom:format"
  },
  "activationHooks": ["language-elixir:grammar-used"],
  "repository": "https://github.com/atom/exfmt-atom",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {}
}
